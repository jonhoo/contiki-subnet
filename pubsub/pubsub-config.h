#include "math.h"

enum reading_type {
  READING_LOCATION,
  READING_HUMIDITY,
  READING_PRESSURE,
};

enum soft_filter  {
  SOFT_FILTER_1
};

enum hard_filter {
  HARD_FILTER_1
};

enum aggregator {
  AGGREGATOR_1
};
